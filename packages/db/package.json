{
  "name": "@condition-tracker/db",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "dependencies": {
    "@condition-tracker/shared": "workspace:*"
  },
  "scripts": {
    "migrate": "tsx ./src/migrate.ts",
    "seed": "tsx ./src/seed.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
