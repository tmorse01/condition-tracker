[build]
builder = "RAILPACK"
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter @condition-tracker/web... build"

[deploy]
startCommand = "pnpm exec serve -s dist -l $PORT"
healthcheckPath = "/"
