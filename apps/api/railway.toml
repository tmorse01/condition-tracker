[build]
builder = "RAILPACK"
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter @condition-tracker/web build && pnpm --filter @condition-tracker/api build"

[deploy]
startCommand = "pnpm --filter @condition-tracker/api start"
healthcheckPath = "/api/health"
