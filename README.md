# Condition Tracker

Lightweight construction loan document workflow demo.

## Structure

- `apps/web` - borrower and internal UI
- `apps/api` - API server and workflow logic
- `packages/shared` - shared domain types
- `packages/db` - schema and seed data
